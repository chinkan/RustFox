# RustFox Axum API base URL used by the Vite dev-server proxy.
# In production Axum serves both /api/* and the built SPA, so this is
# only needed during `npm run dev`.
RUSTFOX_API=http://127.0.0.1:8080
